from setuptools import setup

setup(
    name="notebook",
    version='0.1',
    long_description=__doc__,
    packages=['notebook'],
    include_package_data=True,
    zip_safe=False,
    install_requires=[
    ]
)
